# --- Core runtime dependencies (Phase 1 / RAG line) ---
psycopg[binary]==3.2.3      # Postgres driver (Neon)
pgvector==0.3.6             # pgvector <-> numpy/psycopg adapter
python-dotenv==1.0.1        # load LLM key from .env
anthropic==0.75.0           # Claude: answer generation + batch LLM-as-judge
pydantic==2.13.4            # schemas + lenient validation of LLM tool outputs
langgraph==1.2.11           # StateGraph orchestration of the QA pipeline
langsmith==0.10.18          # optional tracing (enabled via LANGSMITH_TRACING)
streamlit==1.61.1           # demo UI (backend/ui/app.py)
sentence-transformers==3.3.1  # local embeddings (no API key; design-doc §4 option)
numpy==2.1.3               # vector handling for embeddings

# --- Dev / test ---
pytest==8.3.3
pytest-cov==5.0.0
